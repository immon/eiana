<templates>
	<template name="Domain Modification Transaction (Unified Workflow)"
              label="Domain Modification Transaction (Unified Workflow)">
        <field name="name" label="Domain Name"/>
        <section name="supportingOrg" label="Supporting Organization">
            <field name="name" label="Name"/>
            <field name="organization" label="Organization Name"/>
            <list>
                <section name="address" label="Address">
                    <field name="textAddress" label="Text Address"/>
                    <field name="countryCode" label="Country Code"/>
                </section>
            </list>
            <list>
                <field name="phone" label="Phone Number"/>
            </list>
            <list>
                <field name="fax" label="Fax Number"/>
            </list>
            <list>
                <field name="email" label="Email Address"/>
            </list>
        </section>
        <list>
            <section name="adminContact" label="Administrative Contact">
                <field name="name" label="Name"/>
                <field name="organization" label="Organization Name"/>
                <list>
                    <section name="address" label="Address">
                        <field name="textAddress" label="Text Address"/>
                        <field name="countryCode" label="Country Code"/>
                    </section>
                </list>
                <list>
                    <field name="phone" label="Phone Number"/>
                </list>
                <list>
                    <field name="fax" label="Fax Number"/>
                </list>
                <list>
                    <field name="email" label="Email Address"/>
                </list>
            </section>
        </list>
        <list>
            <section name="techContact" label="Technical Contact">
                <field name="name" label="Name"/>
                <field name="organization" label="Organization"/>
                <list>
                    <section name="address" label="Address">
                        <field name="textAddress" label="Text Address"/>
                        <field name="countryCode" label="Country Code"/>
                    </section>
                </list>
                <list>
                    <field name="phone" label="Phone Number"/>
                </list>
                <list>
                    <field name="fax" label="Fax Number"/>
                </list>
                <list>
                    <field name="email" label="Email Address"/>
                </list>
            </section>
        </list>
        <list>
            <section name="nameServer" label="Name Server">
                <field name="name" label="Name Server Host Name"/>
                <list>
                    <field name="ipAddress" label="Name Server IP Address"/>
                </list>
            </section>
        </list>
        <field name="registryUrl" label="Registry URL"/>
        <field name="whoisServer" label="Whois Server"/>
    </template>
</templates>